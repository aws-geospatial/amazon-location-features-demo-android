pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        //TODO remove this once place and routes sdk is live
        maven {
            url = file("${rootDir}/aws-sdk-kotlin-preview/m2").toURI()
        }
    }
}
rootProject.name = "Amazon Location"
include ':app'
